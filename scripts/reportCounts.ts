import { hsk2IndexWords } from "../src/data/hsk2Index";
import { hsk3IndexWords } from "../src/data/hsk3Index";
import { hsk4IndexWords } from "../src/data/hsk4Index";

console.log("Current index counts:");
console.log(`HSK 2: ${hsk2IndexWords.length} words`);
console.log(`HSK 3: ${hsk3IndexWords.length} words`);
console.log(`HSK 4: ${hsk4IndexWords.length} words`);
